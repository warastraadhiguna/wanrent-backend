# WANRent Backend

## Runtime requirement

The backend and its deployment environment require Node.js 20.9.0 or newer.
This minimum is required by Sharp 0.35.x, which validates and re-encodes uploaded
images.

Install production dependencies from the lockfile with:

```sh
npm ci
```
